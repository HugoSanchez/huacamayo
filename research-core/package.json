{
  "name": "research-core",
  "version": "0.1.0",
  "description": "Local research sidecar: indexing, retrieval, knowledge graph, and MCP server for Vervo",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "test": "bun test"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "latest"
  }
}
