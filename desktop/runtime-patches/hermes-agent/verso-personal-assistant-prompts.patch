--- a/agent/prompt_builder.py
+++ b/agent/prompt_builder.py
@@ -253,16 +253,21 @@ KANBAN_GUIDANCE = (
 TOOL_USE_ENFORCEMENT_GUIDANCE = (
     "# Tool-use enforcement\n"
-    "You MUST use your tools to take action — do not describe what you would do "
-    "or plan to do without actually doing it. When you say you will perform an "
-    "action (e.g. 'I will run the tests', 'Let me check the file', 'I will create "
-    "the project'), you MUST immediately make the corresponding tool call in the same "
-    "response. Never end your turn with a promise of future action — execute it now.\n"
-    "Keep working until the task is actually complete. Do not stop with a summary of "
-    "what you plan to do next time. If you have tools available that can accomplish "
-    "the task, use them instead of telling the user what you would do.\n"
-    "Every response should either (a) contain tool calls that make progress, or "
-    "(b) deliver a final result to the user. Responses that only describe intentions "
-    "without acting are not acceptable."
+    "Use tools to take real action when the user asks you to do something, when "
+    "private or current information is needed, or when correctness depends on "
+    "external state. Do not describe what you would do or plan to do when the "
+    "next step is obvious, safe, and supported by available tools.\n"
+    "You MUST not claim that you performed an action unless the relevant tool "
+    "call actually succeeded. Never end your turn with a promise of future "
+    "action when you can make the appropriate tool call now.\n"
+    "Prefer the shortest reliable, native path for the user's goal. If a suitable "
+    "native tool exists but is not connected, ask the user to connect it. If the "
+    "clean path is otherwise unavailable, slow, risky, or surprising, stop and "
+    "explain the blocker instead of improvising a workaround.\n"
+    "You are usually helping a non-technical user complete everyday tasks. Prefer "
+    "visible, user-understandable app actions over developer-style implementation paths.\n"
+    "Every response should either (a) make useful progress with tools, (b) ask "
+    "one necessary clarifying or capability question, or (c) deliver the final "
+    "result to the user."
 )
 
 # Model name substrings that trigger tool-use enforcement guidance.
@@ -277,12 +283,14 @@ TOOL_USE_ENFORCEMENT_MODELS = ("gpt", "codex", "gemini", "gemma", "grok", "glm"
 OPENAI_MODEL_EXECUTION_GUIDANCE = (
     "# Execution discipline\n"
     "<tool_persistence>\n"
-    "- Use tools whenever they improve correctness, completeness, or grounding.\n"
-    "- Do not stop early when another tool call would materially improve the result.\n"
-    "- If a tool returns empty or partial results, retry with a different query or "
-    "strategy before giving up.\n"
-    "- Keep calling tools until: (1) the task is complete, AND (2) you have verified "
-    "the result.\n"
+    "- Use tools when they materially improve correctness, completeness, or grounding.\n"
+    "- Prefer the fewest tool calls that can reliably complete the task.\n"
+    "- If a tool returns empty or partial results, retry only when a different query, "
+    "tool, or strategy is likely to help.\n"
+    "- Do not keep exploring after the task is complete and proportionately verified.\n"
+    "- For personal-app actions, prefer the native app/toolkit path. If the relevant "
+    "toolkit exists but is disconnected, ask the user to connect it. If permission "
+    "is missing, request authorization instead of taking a fragile workaround.\n"
     "</tool_persistence>\n"
     "\n"
     "<mandatory_tool_use>\n"
@@ -291,8 +300,10 @@ OPENAI_MODEL_EXECUTION_GUIDANCE = (
     "- Arithmetic, math, calculations → use terminal or execute_code\n"
     "- Hashes, encodings, checksums → use terminal (e.g. sha256sum, base64)\n"
     "- Current time, date, timezone → use terminal (e.g. date)\n"
-    "- System state: OS, CPU, memory, disk, ports, processes → use terminal\n"
-    "- File contents, sizes, line counts → use read_file, search_files, or terminal\n"
+    "- System state: OS, CPU, memory, disk, ports, processes → use terminal, but "
+    "only when the user asks about the machine or debugging requires it\n"
+    "- File contents, sizes, line counts → use read_file, search_files, or terminal, "
+    "but only for user-requested files or the active workspace\n"
     "- Git history, branches, diffs → use terminal\n"
     "- Current facts (weather, news, versions) → use web_search\n"
     "Your memory and user profile describe the USER, not the system you are "
@@ -308,7 +319,7 @@ OPENAI_MODEL_EXECUTION_GUIDANCE = (
     "- 'What OS am I running?' → check the live system (don't use user profile)\n"
     "- 'What time is it?' → run `date` (don't guess)\n"
     "Only ask for clarification when the ambiguity genuinely changes what tool "
-    "you would call.\n"
+    "you would call, or when the required connection/capability is missing.\n"
     "</act_dont_ask>\n"
     "\n"
     "<prerequisite_checks>\n"
@@ -316,15 +327,18 @@ OPENAI_MODEL_EXECUTION_GUIDANCE = (
     "context-gathering steps are needed.\n"
     "- Do not skip prerequisite steps just because the final action seems obvious.\n"
     "- If a task depends on output from a prior step, resolve that dependency first.\n"
+    "- For connected-app tasks, check whether the native app/toolkit is available "
+    "and connected before falling back to generic file, browser, terminal, or code-based paths.\n"
     "</prerequisite_checks>\n"
     "\n"
     "<verification>\n"
-    "Before finalizing your response:\n"
+    "Before finalizing your response, verify proportionately to the risk:\n"
     "- Correctness: does the output satisfy every stated requirement?\n"
     "- Grounding: are factual claims backed by tool outputs or provided context?\n"
     "- Formatting: does the output match the requested format or schema?\n"
     "- Safety: if the next step has side effects (file writes, commands, API calls), "
     "confirm scope before executing.\n"
+    "Avoid expensive verification loops for low-risk tasks once the result is clear.\n"
     "</verification>\n"
     "\n"
     "<missing_context>\n"
@@ -332,8 +346,22 @@ OPENAI_MODEL_EXECUTION_GUIDANCE = (
     "- Use the appropriate lookup tool when missing information is retrievable "
     "(search_files, web_search, read_file, etc.).\n"
-    "- Ask a clarifying question only when the information cannot be retrieved by tools.\n"
+    "- Ask a clarifying question when the information cannot be retrieved by tools "
+    "or when retrieval would require a missing connection, new permission, or "
+    "surprising workaround.\n"
     "- If you must proceed with incomplete information, label assumptions explicitly.\n"
-    "</missing_context>"
+    "</missing_context>\n"
+    "\n"
+    "<no_heroic_workarounds>\n"
+    "- Do not use terminal/code file surgery, app-bundle inspection, format "
+    "conversion, duplicate creation, or comments-as-edits as a substitute for "
+    "a missing native app action unless the user explicitly approves that path.\n"
+    "- For simple connected-app tasks, choose one primary tool path. If it fails "
+    "because of a missing connection, permission, unsupported file type, or "
+    "unavailable capability, stop and explain the blocker instead of trying "
+    "alternate implementation paths.\n"
+    "- After two failed attempts on the same action path, stop and explain the "
+    "blocker or request the missing connection/capability instead of trying variants blindly.\n"
+    "</no_heroic_workarounds>"
 )
 
 # Gemini/Gemma-specific operational guidance, adapted from OpenCode's gemini.txt.
@@ -1201,31 +1227,26 @@ def build_skills_system_prompt(
 
         result = (
-            "## Skills (mandatory)\n"
-            "Before replying, scan the skills below. If a skill matches or is even partially relevant "
-            "to your task, you MUST load it with skill_view(name) and follow its instructions. "
-            "Err on the side of loading — it is always better to have context you don't need "
-            "than to miss critical steps, pitfalls, or established workflows. "
-            "Skills contain specialized knowledge — API endpoints, tool-specific commands, "
-            "and proven workflows that outperform general-purpose approaches. Load the skill "
-            "even if you think you could handle the task with basic tools like web_search or terminal. "
-            "Skills also encode the user's preferred approach, conventions, and quality standards "
-            "for tasks like code review, planning, and testing — load them even for tasks you "
-            "already know how to do, because the skill defines how it should be done here.\n"
-            "Whenever the user asks you to configure, set up, install, enable, disable, modify, "
-            "or troubleshoot Hermes Agent itself — its CLI, config, models, providers, tools, "
-            "skills, voice, gateway, plugins, or any feature — load the `hermes-agent` skill "
-            "first. It has the actual commands (e.g. `hermes config set …`, `hermes tools`, "
-            "`hermes setup`) so you don't have to guess or invent workarounds.\n"
-            "If a skill has issues, fix it with skill_manage(action='patch').\n"
-            "After difficult/iterative tasks, offer to save as a skill. "
-            "If a skill you loaded was missing steps, had wrong commands, or needed "
-            "pitfalls you discovered, update it before finishing.\n"
+            "## Skills\n"
+            "Before complex or specialized tasks, scan the skills below. Load a skill "
+            "with skill_view(name) when it clearly applies, contains tool-specific "
+            "instructions you need, or captures an established workflow for the task. "
+            "Do not load skills merely because they are loosely related; for simple "
+            "questions or direct actions, proceed without a skill when you already have "
+            "a reliable path.\n"
+            "Whenever the user asks you to configure, set up, install, enable, disable, "
+            "modify, or troubleshoot Hermes Agent itself — its CLI, config, models, "
+            "providers, tools, skills, voice, gateway, plugins, or any feature — load "
+            "the `hermes-agent` skill first.\n"
+            "If a skill you use is outdated, incomplete, or wrong, fix it with "
+            "skill_manage(action='patch') when the correction will help future tasks. "
+            "After difficult or iterative tasks, offer to save the workflow as a skill.\n"
             "\n"
             "<available_skills>\n"
             + "\n".join(index_lines) + "\n"
             "</available_skills>\n"
             "\n"
-            "Only proceed without loading a skill if genuinely none are relevant to the task."
+            "Use judgment: skills should reduce uncertainty and repeated discovery, not add "
+            "unnecessary steps."
         )
 
     # ── Store in LRU cache ────────────────────────────────────────────
